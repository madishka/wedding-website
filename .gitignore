node_modules
.next
out
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
# local RSVP scratch storage (dev only — replaced by Supabase later)
data/rsvps.json
# real guest list + the minted private links — never commit these
scripts/guests.csv
scripts/out/
.gstack/
